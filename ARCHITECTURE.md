# ARCHITECTURE.md — agent-relay 提示词工程解剖

> 本文档从"提示词工程作品"的视角解剖 [SKILL.md](SKILL.md)：它是一份纯指令的 Agent Skill，
> 没有任何脚本或协议（README 原话："No scripts, no protocol — instructions only"）。
> 所有对 SKILL.md 的引用均已与原文核对。

## 一句话定位

agent-relay 解决 **agent 之间 / 会话之间的交接问题**：把一个 git 分支相对基线的全部改动，
蒸馏成一份五段式、人类可读的 Markdown 报告，展示在对话中并落盘到
`.agents/handoff/<branch>-vs-<base>.md`，让下一个 agent（或下一个人）不必读 diff
就能接手这个分支干了什么。

## 输入输出契约

### 消费什么

- **主输入是 git 事实**，不是对话记忆。SKILL.md 第 1 节给出取证命令族：
  - `git merge-base <base> HEAD` — 确定分叉点；
  - `git log --oneline --date=short --pretty=format:'%ad %s' <base>..HEAD` — 拿到带日期的 commit 主题列表；
  - `git diff --stat <base>...HEAD | tail -1` — 拿到文件数与 +/- 行数总账。
- **基线解析有明确的回退链**：默认 `master` → 回退 `main` → 再回退到"用户指定的任何名字"。
- **输入形态可适配**：原文允许"把同样的命令改造"用于——两个任意 ref、一个 commit range、
  或未提交的工作区改动（改用 `git status` 和 `git diff`）。
- **模块命名需要下钻**：对改动最大的目录逐个跑 `git diff --stat <base>...HEAD -- <dir>`，
  以目录为粒度命名模块。

### 产出什么：五段式报告，逐段设计意图

输出模板被完整固定在 SKILL.md 第 2 节（标题为 `# <branch> vs <base> 总结`），五个段落
恰好回答读者接手时的五个递进问题：

| 段落 | 回答的问题 | 设计意图 |
| --- | --- | --- |
| **一句话** | 分支交付了什么、为了解决什么问题 | 最小可信摘要，供人决定"要不要往下读"。约束里刻意强调回答的是"交付了什么"而非"我做了哪些动作"——防止 agent 把过程叙事当成果 |
| **规模** | 改动有多大 | 用 commit 数 / 文件数 / +/- 行数给读者建立心理预算。必须注明 kitex_gen、protobuf、vendored 文件、lock 文件等生成代码占大头的情形，避免"+41k 行"吓退读者（README 示例即此用法） |
| **核心业务流程** | 它是怎么工作的 | 只保留**单一主链**：箭头连接步骤、括号标注关键文件、续行缩进。明确禁止罗列分支和边界情况——主链是接手者最需要的骨架 |
| **主要新增模块** | 加了哪些东西 | 以目录/包为粒度聚合成表格（模块 + 约行数 + 一句职责），而非逐文件清单；既有模块只有"改动实质性大"时才收录 |
| **开发演进** | 它是怎么长成这样的 | 把 commit 时间线归纳为 3–6 个带日期的阶段，每阶段一行。这是"考古线索"：告诉接手者代码为什么长这样，但不展开过程 |

五段的顺序本身就是一次信息设计：从"是什么"（一句话、规模）到"怎么运作"（流程、模块）
再到"从哪来"（演进），漏斗式收紧阅读成本。

### 交付契约（第 3 节 Deliver）

1. 报告先展示在对话里（服务当前会话）；
2. 再持久化到被总结项目的 `.agents/handoff/<branch>-vs-<base>.md`（服务后续会话/其他 agent）；
3. ref 名中的 `/` 清洗为 `-`（避免路径逃逸）；
4. 防御性检查：若项目的 `.gitignore` 未覆盖 `.agents/handoff/`，要明确告知用户——
   防止交接报告被意外提交进仓库。

## 工作流解剖

SKILL.md 的四个编号小节即执行步骤，每步所需的 agent 能力如下：

| 步骤 | agent 做什么 | 需要的工具 |
| --- | --- | --- |
| 1. Collect facts | 解析基线（回退链）→ 跑 log / diff --stat / merge-base → 对最大改动目录下钻 diff --stat → 适配非标准输入（任意 ref 对、commit range、脏工作区） | shell（git）、文件系统读权限 |
| 2. Report format | 按五段模板渲染，使用用户的语言 | 纯语言能力，无工具 |
| 3. Deliver | 展示报告 + 写入 `.agents/handoff/<branch>-vs-<base>.md` + ref 名清洗 + gitignore 检查 | 文件写入、读取 `.gitignore` 验证 |
| 4. Writing constraints | 作为渲染时的质量闸门：数字必须出自命令输出、只报事实不评论 | 无（约束内化于第 2 步的生成过程） |

值得注意的结构：**取证（工具密集）与成文（纯语言）被切成了两个独立阶段**，
第 4 节的写作约束横切在第 2 节之上。这种"先收集后渲染"的分段，等价于把
RAG 的"检索"与"生成"显式分开，便于在约束里做反幻觉控制。

## 提示词设计分析

### frontmatter 触发词设计

description 采用"能力一句话 + 触发场景列表"结构，触发词**中英双语且口语化**：
中文有"总结修改内容、总结分支、看看这个分支改了什么、总结一下改动"，
英文有 "summarize changes"、"branch summary"，并兜底覆盖"想要 diff 或 commit range
的高层摘要"这类意图描述。口语化中文触发词（"看看这个分支改了什么"）比术语式触发词
更贴近真实用户措辞，命中率高。

### 写得好的防御性约束（值得复用的模式）

1. **"Every number in the report comes from command output — never estimate"** —
   最硬的反幻觉条款：禁止估算数字，直接切断了 LLM 最常见的编造路径。
2. **交付物 vs 动作的二分**（"answers '这个分支交付了什么', not '我做了哪些动作'"）——
   用一对对照短语校准摘要的视角，比抽象地说"要抓重点"有效得多。
3. **生成代码豁免条款**：预先枚举 kitex_gen / protobuf / vendored / lock files，
   教 agent 在行数统计失去代表性时主动降级其权重。
4. **聚合优先于枚举**：模块按目录聚合、演进收敛为 3–6 个阶段、流程只留单一主链——
   三处都在对抗 LLM"逐项罗列"的默认倾向，给输出设定了显式的信息压缩比。
5. **"Report facts only — no review, no advice, no process narrative"** —
   三个否定式收尾，把角色钉死在"记录员"上，防止技能漂移成 code review。
6. **落盘副作用的卫生检查**：`/` 清洗 + gitignore 未覆盖时告知，是少见的、
   对文件系统副作用做了安全设计的 Skill。

### 明显缺口与改进建议

1. **退化场景无处理**：基线既无 `master` 也无 `main` 且用户未指名时怎么办？
   分支与基线之间 0 个 commit 时输出什么？建议补一句：
   "若默认基线均不存在，列出远端分支让用户选择；若区间为空，报告空结论而非编造。"
2. **报告体量无预算**：超大型 diff（数千文件）下没有 token 上限约束，五段可能失控膨胀。
   建议加："主要新增模块表格最多列 N 个目录，其余归入'其他'一行。"
3. **演进段落依赖 `%ad`（author date）**：rebase / squash 之后 author date 会与真实
   开发顺序脱节，阶段划分可能错乱。可建议 agent 在日期明显乱序时改用
   `--pretty=format:'%cd %s'`（committer date）复核。
4. **交接的可发现性只靠路径约定**："other agents can find it" 依赖对方知道
   `.agents/handoff/` 这个约定，没有索引机制。可在写报告时顺带维护一个
   `.agents/handoff/INDEX.md`，或建议把路径约定写进项目的 AGENTS.md。
5. **覆盖策略未定义**：同一 branch-vs-base 再次总结时，是覆盖旧报告还是另存？
   旧报告中的过期结论可能误导接手者。建议明确"覆盖并可在文首标注生成时间与 HEAD"。
6. **ref 名清洗只处理 `/`**：含空格、`..` 或 Windows 非法字符的 ref 名未覆盖，
   可扩展为"清洗所有文件系统不安全字符"。
7. **缺反向触发（何时不适用）**：description 没有负面边界，如"解释某个单 commit"
   或"做 code review"不应触发本 Skill，容易误命中"总结一下改动"级别的单文件修改请求。

## 与同类机制的关系

| 机制 | 记忆来源 | 生命周期 | agent-relay 的差异 |
| --- | --- | --- | --- |
| **上下文压缩（compaction）** | 当前会话对话历史的摘要 | 会话内，随会话失效 | 压缩的是"对话"，agent-relay 蒸馏的是"代码仓库客观状态"，以 git 输出为锚，不依赖也没继承对话里的幻觉 |
| **session handoff / 会话交接提示** | 上一个 agent 写下的自由文本交接 | 跨会话，但质量取决于写者自觉 | agent-relay 是**模板化、带反幻觉约束**的交接：五段结构与数字来源被硬性规定，把"交接质量"从个人风格变成可复现的工序 |
| **AGENTS.md / CLAUDE.md** | 仓库维护的常驻指令 | 长期，描述"规则" | 描述的是不变量；agent-relay 产出的是**时点状态快照**（这个分支此刻改了什么），二者互补：快照过期、规则常驻 |
| **git log / diff 本身** | 仓库原始数据 | 永久 | 原始数据无人读得动；agent-relay 站在"git 事实"与"下一个 agent 的上下文窗口"之间，做一次带契约的有损压缩 |

**生态位总结**：它填补的是"会话死了、分支还活着"的空隙——上下文压缩管会话内延续，
AGENTS.md 管长期规则，而跨会话、跨 agent、基于代码客观状态的交接物，
由这份落盘的五段报告承担。以文件为媒介（而非协议），意味着任何能读文件的
agent runtime 都能消费，零集成成本。

## 评分

**8 / 10**。契约清晰、反幻觉约束扎实、输出模板可直接复用；扣分在退化场景
（空区间、无默认基线）、报告体量预算和交接可发现性上存在真实的使用缺口。
